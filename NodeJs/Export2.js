// Export from directory -> we can use pre-writing all code from a directory.
// see Fruits/index.js

const data = require("./Fruits");
console.log(data);
console.log(data[0]);
console.log(data[0].color);
