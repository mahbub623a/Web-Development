// what is node js? -> javascript Runtime environment. Give the capability to run javascript outside the browser.
// It is used for server side programming
// * Node.js is not a language, library or framework, it is a Runtime environment

// Node REPL -> REPL stands for Read, Evaluate, Print and loop
// It gives us browser like functionality to our shell. write "node" in terminal to activate this feature
// In browser we had window object(which is the part of DOM) but here we have global(mostly similar with window object)

// Run file in nodeJs -> node filename.js
let n = 5;
for (let i = 0; i < n; i++) {
    console.log(i, "hello");
}
console.log("bye!");



